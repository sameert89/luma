CREATE TABLE Tags(Id INTEGER PRIMARY KEY,Name TEXT NOT NULL,NormalizedKey TEXT NOT NULL UNIQUE,NormalizationVersion INTEGER NOT NULL DEFAULT 1);
CREATE TABLE MediaTags(MediaId INTEGER NOT NULL REFERENCES Media(Id),TagId INTEGER NOT NULL REFERENCES Tags(Id),PRIMARY KEY(MediaId,TagId));
CREATE INDEX IX_MediaTags_Tag ON MediaTags(TagId,MediaId);
