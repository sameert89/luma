{
  "rows": 1000000,
  "synthetic": true,
  "sqlOnly": true,
  "sqliteVersion": "3.49.1",
  "parameters": [
    "present",
    "image",
    124999
  ],
  "queries": {
    "before": {
      "sql": "SELECT m.* FROM Media m WHERE m.Availability=? AND (m.MediaType,m.Id)<(?,?) ORDER BY m.MediaType DESC,m.Id DESC LIMIT 201",
      "plan": [
        "SEARCH m USING INDEX IX_Media_TypeOrder (Availability=? AND MediaType<?)"
      ]
    },
    "after": {
      "sql": "SELECT m.* FROM Media m WHERE m.Availability=? AND m.MediaType=? AND m.Id<? ORDER BY m.Id DESC LIMIT 201",
      "plan": [
        "SEARCH m USING INDEX IX_Media_TypeOrder (Availability=? AND MediaType=? AND Id<?)"
      ]
    }
  }
}