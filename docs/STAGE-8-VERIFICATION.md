# Stage 8 v1.0.2 verification

Local Windows verification on 2026-09-19, using .NET 10 and Node 24.21.0. PowerShell commands ran without the user profile; Node selection used fnm env/use directly.

| Command (repository root) | Result |
| --- | --- |
| dotnet build Luma.slnx -c Release | Passed, zero warnings/errors |
| dotnet test tests/Luma.Server.Tests -c Release --no-build | 88 passed, 1 Windows symlink test skipped, 0 failed |
| npm --prefix src/Luma.Web run generate:api | Passed; OpenAPI and TypeScript regenerated |
| npm --prefix src/Luma.Web run build | Passed |
| npm --prefix src/Luma.Web test -- --reporter=dot --maxWorkers=2 | 67 passed across 15 files |
| npm --prefix src/Luma.Web run lint | Passed |
| git -c core.safecrlf=false diff --check | Passed |

E2E/Playwright tests were not run, as explicitly requested. Browser-unit media tests emit jsdom's existing unimplemented pause/load notices; these are not test failures.

Regression coverage includes loading/empty/refetch states, visible scope reset and shared query values, URL restoration of an open file, repeated/nested modal lock cleanup, XMP additions/changes/removals and unchanged-source skips, forced manual tag repair, interrupted selected/query import recovery without count reset, indexing mode enqueue and task visibility, cancelled-job restart persistence, running-import cancellation, checkpoint resumption, scan requeue scope/mode preservation, queue controls, compact gallery actions/filter toggle, pending root folder listing and accepted scan status loading, any-tag collection intersection without original access, SQLite watch history shared across clients, seeking/accidental-playback completion thresholds, photo pinch/double-tap/pan navigation exclusion, normal-player-only volume swipes and preserved right-side Reels navigation, resume/start-over, and filtered Random API URL generation.

Intermediate checks found and resolved a missing-sidecar fingerprint error, outdated migration/filter assertions and an accessibility lint error on a custom range label. The default parallel frontend run hit two 5-second App test timeouts; the App file passed in isolation and the full 65-test suite passed with --maxWorkers=2. Two later backend runs hit a Windows original-file deletion lock in OriginalContentTests (82 passed, 1 failed, 1 skipped), after an earlier 83-pass run. That test now prepares cached previews before starting hosted workers, preventing its source-deletion assertion from racing the unrelated image decoder; final backend verification follows that fixture correction. One intermediate build also hit a testhost assembly copy lock while tests were still running; builds succeeded once that run finished. An initial generate:api invocation from the repository root failed because package.json belongs to src/Luma.Web; the documented prefixed command succeeded.

No real-device long-session soak or Raspberry Pi resource/latency benchmark was performed. The scroll-lock changes target closed modal presence and unconstrained gallery flex sizing; unit coverage proves cleanup in the tested cycles but does not prove that every cause of the user's intermittent device freeze has been eliminated. Device playback/volume restrictions and two-finger gestures still need target-client acceptance. Watch progress is instance-wide, saved periodically/on exit; abrupt termination can lose the last save interval. XMP changes are discovered on the next index/import pass.

Historical HTTP/PWA follow-up (the Settings panel was subsequently removed at the user’s request): Settings reported secure-context installation availability, captures native install offers before Settings opens, offers manual home-screen guidance for HTTP, and shows service-worker startup registration failures. The full frontend run passed 69 tests; the final added startup-error case and all five installation tests passed in the focused run. Both final builds and lint passed, backend remained 83 passed/1 skipped, and E2E remained excluded. Browser/OS installation restrictions cannot be bypassed by the app; no actual LAN-device installation was performed. Deployment instructions distinguish same-device HTTP localhost, HTTP LAN shortcuts and trusted HTTPS, and fix the private Caddy example to use tls internal with explicit client CA trust.

Folder-sort follow-up: folders previously ignored the media sort and always used database ID order. The UI now requests indexed case-insensitive name sorting in the visible direction, while date/type/size/shuffle remain explicitly media-only. Migration 0014 adds the composite index; the default API ID-ascending behavior remains compatible. Backend regressions cover ascending/descending multi-page traversal, tied names, previous-page navigation, cursor direction mismatch, hidden folders, offline source browsing, and EXPLAIN QUERY PLAN index use without a temporary sort. The UI test checks transmission of ascending direction. Final commands in the table passed: backend 85 passed/1 skipped, frontend 70 passed, both builds, API generation, lint and diff check. No E2E ran.

Queue and Settings cleanup: the installation panel and its prompt interception were removed; README and deployment instructions retain browser installation guidance. Clear finished persistently dismisses terminal queue entries while retaining scan history and metadata checkpoints, leaves queued/running work and active media preparation visible, and allows dismissed cancelled imports to be requeued. Migration 0015 adds persistent dismissal and queue indexes. The top tag/filter row hides its scrollbar while retaining horizontal scrolling. Backend and frontend regression tests cover queue clearing and active-item retention. An initial queue test stopped hosted services after startup and disposed the test server; removing those workers before host startup fixed the fixture. One frontend run discovered the installation test before its removal and reported a missing suite; the final full run after removal passed all 66 tests. Final backend verification passed 86 tests with one skip; both builds, API generation and lint passed. E2E remained excluded.

Settings/cancellation follow-up: build and test commands in the table were rerun after compact two-column theme previews, matched Settings headings, a sticky queue-clear toolbar, and confirmed-request-cancellation handling for SQLite error 9. Regression tests distinguish cancelled from uncancelled SQLite interruptions and verify Settings headings without an installation panel. Help placement remains a design discussion; no new Help section was implemented.
